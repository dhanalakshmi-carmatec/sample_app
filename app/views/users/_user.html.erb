<tr>
  <td><%= user.name %></td>
  <td><%= user.email %></td>
  <td><%= user.active %></td>
  <td class="actions">
    <%= link_to 'Edit', edit_user_path(user) %>
    <%= link_to 'Delete', user_path(user), method: :delete, data: { confirm: 'Are you sure?' } %> 
    <%= link_to 'UserDetails', user_user_details_path(user, @user_detail)%> 
  </td>
</tr>
