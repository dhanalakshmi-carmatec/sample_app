<tr>
  <td><%= book.name %></td>
  <td><%= book.description %></td>
  <td class="actions">
    <%= link_to 'Edit', edit_category_book_path(@category, book) %>
    <%= link_to 'Delete', category_book_path(@category, book), method: :delete, data: { confirm: 'Are you sure?' } %>    
  </td>
</tr>
